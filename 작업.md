# 7회차 프로젝트 기준 정렬 작업 계획

## 1. 문서 목적

현재 프로젝트를 다음 7회차 프로젝트와 기능·데이터·라우팅 구조 측면에서 맞추기 위한 작업 계획입니다.

- 현재 프로젝트: `C:\Users\fujifilm\Documents\4회차 실습\superSCM-main\superSCM`
- 기준 프로젝트: `C:\Users\fujifilm\Documents\7회차 실습\superSCM-main\superSCM-main`

이번 비교에서는 파일을 복사하거나 수정하지 않았습니다. 특히 `.worktrees` 내부 파일은 비교 대상에서 제외했습니다.

## 2. 현재 상태와 기준 상태

현재 프로젝트는 인증·RBAC, 기존 Workflow, Agent 기본 구조가 중심입니다. 기준 프로젝트는 실데이터 전환을 전제로 하며 Step 3~7의 데이터 격리·수요 프로파일·Forecast·Backtest·Champion 기능과 관리자 운영 화면이 추가되어 있습니다.

| 영역 | 현재 프로젝트 | 기준 프로젝트 |
|---|---|---|
| DB Migration | Step 2까지 | Step 3~7, Agent Conversation까지 |
| 사용자 화면 | Workflow와 기본 분석 | Dashboard와 실데이터 분석 |
| 관리자 화면 | 사용자·마스터 관리 | 적재·수요·Forecast·Backtest·Champion 관리 |
| 데이터 접근 | 기존 `raw/core/analytics` 뷰 | 실데이터 `dim/fact/bridge`와 analytics 뷰 |
| Agent | UI·Tool·LLM·대화 저장 기반 | API 스트리밍과 실데이터 연결 보강 |
| 메뉴 | 수요 확정·재고 공급 등 Workflow 중심 | 수요 패턴·OL 정확도·운영 기능 중심 |
| 파일 업로드 | 미완성 또는 제한적 | CSV/Excel 파싱·검증·Commit·Rollback |

## 3. 1순위: DB 구조와 Migration 정렬

현재 프로젝트에 다음 Migration이 없습니다.

```text
20260828000200_step3_data_isolation.sql
20260828000300_step4_import_pipeline.sql
20260828000400_step5_sku_demand_profile.sql
20260828000500_step6_baseline_forecast.sql
20260828000600_step7_backtest_champion.sql
20260909000100_step16_agent_conversation.sql
```

다음 객체를 우선 확인하고 추가합니다.

- `core.forecast_setting`
- `core.v_train_demand`
- `core.v_test_actual`
- `analytics.v_data_coverage`
- `analytics.v_sku_demand_profile`
- `core.forecast_run`
- `core.forecast_result`
- `core.backtest_run`
- `core.model_performance`
- `core.champion_model`
- `core.agent_conversation`
- `core.agent_message`

### 주의사항

기준 Migration을 그대로 실행하지 않습니다. 현재 Supabase의 실제 테이블·컬럼·View 존재 여부를 먼저 확인합니다.

- 기존 테이블을 Drop/Recreate하지 않습니다.
- 이미 존재하는 객체와 이름이 겹치는지 확인합니다.
- `raw` 원본 데이터를 직접 수정하지 않습니다.
- `core`는 정책과 계산 기준, `analytics`는 화면 조회 결과로 유지합니다.
- `anon` 쓰기 권한과 과도한 `using(true)` 정책이 없는지 확인합니다.

## 4. 2순위: 실데이터 조회 계층 전환

`lib/scm-model.ts`에 기준 프로젝트의 실데이터 타입과 정규화 함수를 추가합니다.

주요 View:

```text
analytics.v_shipment_trend
analytics.v_item_demand_profile
analytics.v_item_demand_kpi
analytics.v_ol_accuracy
analytics.v_ol_accuracy_fy
analytics.v_bom_requirement_x
```

`lib/scm.ts`는 위 View만 조회하도록 정리합니다.

필요한 조회 함수:

```text
getItemDemandProfiles()
getItemDemandKpi()
getShipmentTrends()
getOlAccuracy()
getOlAccuracyFy()
getBomRequirements(modelBase)
```

### 호환성 원칙

- 기존 `getLeadtimeGap`, `getStockoutRisk`, `getStockoutKpi`는 필요한 화면이 있으므로 즉시 삭제하지 않습니다.
- 기존 Agent Tool이 사용하는 함수명은 alias 또는 단계적 변경으로 호환성을 유지합니다.
- 조회 실패는 예외 대신 `{ rows: [], error }`로 반환합니다.
- 없는 값은 `0`이 아니라 `null`과 `reason_code`로 유지합니다.
- 화면과 Agent가 동일한 조회 함수를 사용합니다.
- `raw` 테이블을 화면이나 Agent에서 직접 조회하지 않습니다.

## 5. 3순위: 사용자 Route 정리

다음 Route를 추가합니다.

```text
/dashboard
/analysis/demand-profile
/analysis/model-comparison
```

현재 `/`의 기존 Workflow 대시보드는 기준 프로젝트처럼 `/dashboard`로 분리하고, 루트는 `/dashboard`로 redirect하는 구조를 검토합니다.

기존 기능 호환을 위해 다음 Route는 당장 삭제하지 않습니다.

```text
/workflow
/analysis/leadtime
/analysis/stockout
```

실데이터가 아직 없는 화면에는 빈 값을 임의로 채우지 말고 `NoRealDataNotice`와 계산불가 상태를 표시합니다.

## 6. 4순위: 관리자 운영 화면

다음 관리자 Route를 추가합니다.

```text
/admin/workflow
/admin/demand
/admin/data-management
/admin/forecast-models
/admin/forecast-runs
/admin/backtest-runs
/admin/champion-models
/admin/settings
```

주요 컴포넌트:

```text
components/admin/import-manager.tsx
components/admin/import-wizard.tsx
components/admin/import-history-table.tsx
components/admin/forecast-models-table.tsx
components/admin/forecast-runs-table.tsx
components/admin/backtest-runs-table.tsx
components/admin/champions-table.tsx
components/admin/forecast-settings-panel.tsx
```

모든 관리자 변경은 다음을 지킵니다.

- Server Action 또는 Route Handler 첫 부분에서 `requireAdmin()` 호출
- DB RLS에서도 ADMIN 확인
- 변경 전후 값을 `audit_log`에 기록
- 화면에서 메뉴를 숨기는 것만으로 권한을 대체하지 않음

## 7. 5순위: 파일 적재 Pipeline

CSV/Excel 업로드가 필요하므로 다음 패키지를 검토합니다.

```json
"papaparse"
"xlsx"
"@types/papaparse"
```

적재 흐름은 다음과 같이 구성합니다.

```text
파일 업로드
  → 파싱
  → 컬럼·타입·참조 검증
  → 오류 행 보고
  → 관리자 Commit
  → Import 이력 저장
  → 필요 시 Rollback
```

적재 API 후보:

```text
/api/admin/imports/parse
/api/admin/imports/validate
/api/admin/imports/commit
/api/admin/imports/rollback
```

## 8. 6순위: Agent 운영 연결

현재 Agent 핵심 로직은 이미 존재하므로 기능을 새로 확장하기보다 기준 프로젝트처럼 운영 경로를 보강합니다.

추가 대상:

```text
app/api/agent/stream/route.ts
app/(user)/agent/answer-card.tsx
```

연결 구조:

```text
Agent 화면
  → /api/agent/stream
  → orchestrator
  → role별 허용 Tool
  → analytics View 조회
  → Structured Answer
  → conversation/message 저장
```

보안 원칙:

- OpenAI API Key는 서버에서만 읽습니다.
- Agent에서 Supabase를 직접 조회하지 않습니다.
- `lib/scm.ts` 조회 함수만 Tool에서 호출합니다.
- 사용자 role은 Tool 노출 시점과 실행 직전에 모두 확인합니다.
- Tool 원본 데이터 전체를 클라이언트에 보내지 않습니다.

## 9. 7순위: 메뉴 구조 정리

`lib/menu.ts`를 기준 프로젝트처럼 다음 구조로 정리합니다.

```text
USER_MENU
ADMIN_MENU
menuForRole(role)
```

USER 메뉴 후보:

```text
전체 현황
수요 패턴
OL 예측 정확도
리드타임 격차
재고 소진 위험
AI 비서
```

ADMIN 메뉴 후보:

```text
사용자 관리
발주계획 관리
수요 관리
데이터 관리
Forecast Models
Forecast Runs
Backtest Runs
Champion Models
시스템 설정
```

기존 수요 확정·재고 공급·마스터 검증·발주량 계산·보고자료 Workflow는 레거시 호환 경로로 유지한 뒤, 실데이터 기능과 섞이지 않도록 단계적으로 분리합니다.

## 10. 디자인/CSS 작업 방향

현재 프로젝트는 다크 테마와 기존 Workflow 스타일이 크게 구성되어 있습니다. 기준 프로젝트는 `globals.css`를 작게 유지하고 다음 파일에 상세 스타일을 분리합니다.

```text
app/globals.css
styles/shell.css
styles/components.css
styles/chart.css
```

CSS 전체 교체는 기능 전환 후 진행합니다.

- 기존 Dark 디자인을 유지할지 먼저 결정합니다.
- 화면의 직접 hex 색상 사용을 금지합니다.
- 상태 색상은 공통 토큰을 사용합니다.
- `SAFE`, `WARNING`, `CRITICAL`, `CALCULATION_UNAVAILABLE`를 통일합니다.
- 계산불가 값은 `— + reason_code`로 표시합니다.

## 11. 실행 순서

### Phase 1 — DB 확인

- [ ] 현재 Supabase 테이블·View·스키마 노출 확인
- [ ] Step 3~7 Migration 충돌 여부 확인
- [ ] RLS와 권한 검토
- [ ] 실제 데이터의 `dim/fact/bridge` 구조 확인

### Phase 2 — 조회 계약

- [ ] `lib/scm-model.ts` 타입 추가
- [ ] 정규화 함수와 null 정책 추가
- [ ] `lib/scm.ts` 실데이터 조회 함수 추가
- [ ] Agent Tool 연결 및 기존 함수 호환성 확인

### Phase 3 — 화면

- [ ] `/dashboard` 추가
- [ ] 수요 패턴 화면 추가
- [ ] OL 정확도 화면 추가
- [ ] 관리자 운영 화면 추가
- [ ] 메뉴를 USER/ADMIN으로 재구성

### Phase 4 — 적재·Agent 운영

- [ ] CSV/Excel 파싱·검증
- [ ] Commit/Rollback
- [ ] Import 이력
- [ ] Agent 스트리밍 API
- [ ] 대화 저장 연결

### Phase 5 — 검증·배포

- [ ] `npm test`
- [ ] `npx tsc --noEmit`
- [ ] `npm run build`
- [ ] Supabase 실제 View 조회 확인
- [ ] USER/ADMIN 권한 테스트
- [ ] Vercel 환경변수와 API 스키마 노출 확인

## 12. 완료 기준

- 실데이터 View에서 수요·출고·OL·BOM 결과를 조회할 수 있습니다.
- 화면과 Agent가 동일한 조회 계층을 사용합니다.
- Forecast 학습 데이터와 검증 데이터가 분리됩니다.
- USER가 관리자 Route나 Tool을 실행할 수 없습니다.
- 관리자의 변경 이력이 저장됩니다.
- CSV/Excel 적재 결과와 오류 행을 재조회할 수 있습니다.
- 기존 레거시 Workflow가 새 실데이터 화면과 충돌하지 않습니다.
- 계산불가 값을 숫자 `0`으로 대체하지 않습니다.
- 테스트·TypeScript·빌드가 모두 통과합니다.

## 13. 최종 권장 사항

가장 먼저 화면을 복사하지 말고 다음 순서로 작업합니다.

1. 현재 Supabase 실제 구조와 기준 Migration의 충돌 확인
2. Step 3~7 DB 객체 반영
3. `lib/scm.ts`와 `lib/scm-model.ts`를 실데이터 기준으로 전환
4. Dashboard·분석·관리자 Route 추가
5. 메뉴와 Agent 운영 연결
6. 업로드 Pipeline과 배포 검증

기준 프로젝트의 파일을 한 번에 덮어쓰면 기존 인증·Agent·다크 디자인·레거시 화면이 깨질 수 있으므로, 단계별로 추가하고 각 단계마다 테스트와 빌드를 수행합니다.
