# superSCM 전반 시스템 디자인 가이드

> 월간 발주계획 시스템의 전체 화면을 하나의 시각 언어로 통일하기 위한 디자인 기준입니다.
> 이 문서는 제공된 다크 운영 대시보드 레퍼런스를 바탕으로 하되, 프로젝트의 Next.js·React·순수 CSS·Supabase 구조에 맞게 조정한 실행 기준입니다.
>
> 상태: 승인된 디자인 방향
> 기준일: 2026-08-28
> 적용 대상: `/` 업무 플로우, `/analysis/*` 분석 화면, 향후 입력·계산·보고 화면

## 1. 디자인 방향 요약

### 한 문장 정의

**어두운 운영 관제 화면 위에 밝은 데이터와 명확한 상태 신호를 배치하는, 고밀도이지만 차분한 SCM 업무 대시보드**를 지향합니다.

### 레퍼런스에서 가져오는 요소

- `#131313` 계열의 깊은 다크 배경
- 배경 위에 단계적으로 쌓이는 surface panel
- 얇은 테두리와 반투명 패널을 활용한 글래스 느낌
- 블루를 기본 강조색, 레드를 위험·예외색, 민트를 정상·완료색으로 사용하는 상태 체계
- 큰 숫자와 짧은 라벨을 조합한 KPI 표현
- 넉넉한 외곽 여백과 12-column 기반의 대시보드 그리드
- 작은 대문자 eyebrow와 명확한 제목 계층
- 둥근 pill 탭, compact 버튼, 짧은 전환 애니메이션

### 프로젝트에 맞게 바꾸는 요소

| 레퍼런스 | superSCM 적용 |
|---|---|
| Tailwind utility class | `app/globals.css`의 전역 CSS 변수·클래스 |
| Material Symbols | 현재 사용 중인 `lucide-react` |
| 영어 운영 용어 | 업무 사용자가 이해하는 한국어, 필요한 경우 영문 보조 라벨 |
| Incident 중심 카드 | 발주계획·수요·재고·리드타임·예외 중심 카드 |
| 장식용 이미지 | 실제 업무 정보가 우선이며 이미지는 최소화 |
| 임의 차트·숫자 | DB/분석 결과의 출처와 상태를 함께 표시 |

## 2. 적용 범위와 변경 원칙

### 적용 범위

1. 전역 배경, 색상, 글꼴, 간격, 모서리, 그림자 토큰
2. 홈 업무 플로우의 sidebar·topbar·progress·card·table·form
3. 분석 화면의 analysis shell·tabs·KPI·표·오류/빈 상태
4. 향후 수요 입력, 재고 입력, 계산 결과, 보고자료 화면

### 유지해야 할 구조

- Next.js App Router와 현재 라우트 구조를 유지합니다.
- `components/workflow/`의 여섯 단계 컴포넌트는 업무 의미와 데이터 흐름을 유지합니다.
- 분석 화면은 `lib/scm-model.ts` → `lib/scm.ts` → `app/analysis/<기능>/page.tsx` 순서를 유지합니다.
- Supabase의 `raw`, `core`, `analytics` 역할을 바꾸지 않습니다.
- 화면에서 숫자 계산을 새로 만들지 않습니다. 계산 결과는 SQL 또는 모델 계층에서 받습니다.
- Tailwind, CSS Modules, styled-components 등 새 스타일 시스템을 도입하지 않습니다.

### 우선순위

정보의 정확성 > 상태 식별성 > 읽기 편의성 > 시각적 장식

다크 테마를 적용하더라도 표의 수치, 오류 메시지, 입력값, 확정 상태는 장식보다 잘 읽혀야 합니다.

## 3. 시각 언어

### 3.1 색상 토큰

아래 토큰은 `app/globals.css`의 `:root`에 정의할 기준입니다. 기존 클래스가 직접 색상값을 갖지 않고 가능한 한 토큰을 참조하도록 합니다.

#### 기본 색상

| 토큰 | 값 | 용도 |
|---|---|---|
| `--color-bg` | `#131313` | 전체 앱 배경 |
| `--color-surface-lowest` | `#0e0e0e` | 가장 깊은 배경, 입력 내부 |
| `--color-surface-low` | `#1c1b1b` | sidebar·낮은 대비 영역 |
| `--color-surface` | `#201f1f` | 기본 카드·표면 |
| `--color-surface-high` | `#2a2a2a` | hover·선택·강조 카드 |
| `--color-surface-highest` | `#353534` | 활성 입력·활성 탭 |
| `--color-on-surface` | `#e5e2e1` | 기본 본문·제목 |
| `--color-on-surface-muted` | `#c1c6d7` | 보조 본문·설명 |
| `--color-outline` | `#8b90a0` | 비활성 텍스트·약한 테두리 |
| `--color-outline-variant` | `#414755` | 구분선·패널 테두리 |

#### 의미 색상

| 의미 | 기본 토큰 | 밝은 텍스트 | 적용 예 |
|---|---|---|---|
| 주요 동작 | `--color-primary: #adc6ff` | `--color-on-primary: #002e69` | 선택, 주요 버튼, 링크 |
| 주요 패널 | `--color-primary-container: #4b8eff` | `--color-on-primary-container: #00285c` | 핵심 KPI, 주요 안내 |
| 위험·예외 | `--color-danger: #de0541` | `--color-on-danger: #fff1f1` | 재고 위험, Flex 초과, 오류 |
| 정상·완료 | `--color-success: #00dfc1` | `--color-on-success: #00201a` | 정상, 확정, 저장 완료 |
| 주의·대기 | `--color-warning: #ffb4ab` | `--color-on-warning: #690005` | 입력 대기, 표본 부족, 검토 |

주의 색상은 색상 하나만으로 의미를 전달하지 않습니다. 반드시 텍스트, 아이콘, 수치 또는 상태 라벨을 함께 표시합니다.

#### 상태 매핑

| 상태 | 시각 처리 | 문구 예 |
|---|---|---|
| 정상/완료 | 민트 배경 12~18% + 민트 텍스트 + check 아이콘 | `정상`, `확정 완료` |
| 진행 중 | 블루 배경 12~18% + 블루 텍스트 | `작성 중`, `검증 중` |
| 대기/주의 | 앰버·연분홍 계열 배경 + 경고 아이콘 | `입력 대기`, `확인 필요` |
| 위험/예외 | 레드 배경 16~22% + 경고 아이콘 | `Flex 초과`, `위험` |
| 미정/계산 불가 | 중립 surface + `—` 또는 사유 코드 | `미정`, `NO_USAGE` |

### 3.2 패널과 글래스 효과

기본 카드는 완전히 투명하지 않은 surface를 사용합니다. 배경과 카드가 충분히 구분되어야 하므로 투명도만으로 카드를 표현하지 않습니다.

```css
.panel {
  background: var(--color-surface);
  border: 1px solid color-mix(in srgb, var(--color-outline-variant) 32%, transparent);
  border-radius: var(--radius-lg);
}

.panel-glass {
  background: color-mix(in srgb, var(--color-surface-high) 72%, transparent);
  border: 1px solid color-mix(in srgb, white 7%, transparent);
  backdrop-filter: blur(18px);
}
```

`backdrop-filter`는 보조 효과입니다. 지원하지 않는 브라우저에서도 불투명 surface로 읽혀야 합니다. 모든 카드에 강한 glow나 큰 그림자를 넣지 않습니다.

### 3.3 타이포그래피

레퍼런스의 Inter/Hanken Grotesk 방향을 참고하되, 한국어 글리프와 프로젝트의 기존 로딩 방식을 고려합니다.

| 역할 | 기준 | 사용처 |
|---|---|---|
| Display | 40~48px / 700 / line-height 1.15 | 핵심 KPI 숫자 |
| Headline | 28~32px / 700 / line-height 1.25 | 페이지 제목 |
| Section title | 18~20px / 600 / line-height 1.4 | 카드·섹션 제목 |
| Body | 14px / 400~500 / line-height 1.55 | 설명·표 본문 |
| Label | 11~12px / 600 / letter-spacing 0.04em | eyebrow·상태·보조 라벨 |
| Numeric | tabular-nums | 수량·금액·일수·비율 |

원칙:

- 한국어 본문은 현재 프로젝트의 `Noto Sans KR`을 기본으로 사용합니다.
- 영문 kicker와 숫자는 Inter 계열이 적합하지만, 새 외부 폰트 의존성은 추가하지 않습니다.
- 제목은 짧고 한 줄 우선으로 작성합니다.
- 금액·수량·일수는 `font-variant-numeric: tabular-nums`를 적용해 세로 비교를 쉽게 합니다.
- 대문자 영문은 eyebrow·상태 코드처럼 짧은 메타 정보에만 사용합니다.

### 3.4 모서리·간격·그림자

| 토큰 | 값 | 용도 |
|---|---:|---|
| `--radius-sm` | `8px` | 입력, 작은 배지 |
| `--radius-md` | `12px` | 버튼, 작은 카드 |
| `--radius-lg` | `16px` | 기본 카드, 표 컨테이너 |
| `--radius-xl` | `24px` | 강조 패널, hero 영역 |
| `--radius-pill` | `999px` | 탭, 상태 배지, progress |
| `--space-1` | `4px` | 아이콘-텍스트 최소 간격 |
| `--space-2` | `8px` | 행 내부 간격 |
| `--space-3` | `12px` | compact padding |
| `--space-4` | `16px` | 기본 내부 padding |
| `--space-6` | `24px` | 카드 padding·그리드 gap |
| `--space-8` | `32px` | 섹션 간격 |
| `--space-12` | `48px` | 큰 화면 외곽 여백 |

그림자는 밝은 glow보다 표면 높이를 구분하는 데 사용합니다.

- 기본 카드: 약한 `0 1px 2px rgba(0,0,0,.25)`
- hover: 약한 색상 테두리와 `0 8px 24px rgba(0,0,0,.18)`
- 강조 패널: 필요한 경우에만 primary glow를 20px 이내로 사용
- 표·입력: 그림자보다 테두리와 표면 차이를 우선

## 4. 레이아웃 시스템

### 4.1 데스크톱 구조

현재의 좌측 업무 네비게이션 구조는 유지하되, 시각적으로는 하나의 운영 콘솔처럼 구성합니다.

```text
┌──────────────┬──────────────────────────────────────────────┐
│ sidebar      │ topbar                                       │
│ 240~264px    ├──────────────────────────────────────────────┤
│              │ content                                      │
│ 업무 플로우  │ max-width 1440~1600px                         │
│ 분석 링크    │ 12-column grid                                │
└──────────────┴──────────────────────────────────────────────┘
```

- sidebar는 고정 폭 `250px`를 기준으로 하되 콘텐츠가 잘리지 않게 합니다.
- topbar는 sticky가 필요할 때만 사용하고, 업무 화면의 핵심 제목과 기준월도 표시합니다.
- 본문 최대 폭은 `1440px`, 대형 모니터에서 `1600px`까지 허용합니다.
- 데스크톱 기본 외곽 여백은 32~48px, 카드 간 gap은 16~24px입니다.
- 12-column 그리드는 KPI, 요약, 표, 안내 패널을 조합할 때 사용합니다.

### 4.2 반응형 기준

| 구간 | 기준 |
|---|---|
| 1280px 이상 | sidebar + 12-column dashboard |
| 900~1279px | sidebar 축소 또는 내부 네비게이션 전환, 6-column |
| 640~899px | sidebar를 상단/접이식 영역으로 변경, 2-column |
| 639px 이하 | 단일 column, 표 가로 스크롤, 주요 KPI 세로 배치 |

모바일에서는 정보 자체를 숨기기보다 우선순위가 낮은 보조 라벨을 접습니다. 테이블은 열을 임의로 삭제하지 않고 가로 스크롤 또는 행 상세 패턴을 사용합니다.

### 4.3 그리드 사용 규칙

- KPI 4개: `grid-4`, 작은 화면에서 `grid-2` → `grid-1`
- 요약 + 주요 분석: `grid-3` 또는 `3/6/3` 비율
- 표 + 안내 카드: 표가 최소 2/3 폭을 차지하도록 배치
- 빈 화면: 전체 폭의 중앙 정렬보다 왼쪽 정렬 본문과 충분한 상하 여백을 우선
- 카드 높이는 억지로 동일하게 맞추지 않고, 같은 행 안에서만 시각적으로 정렬

## 5. 공통 컴포넌트 규칙

### 5.1 Sidebar

대상: `components/procurement-app.tsx`의 `.sidebar`

- 브랜드는 짧은 서비스명, 보조 설명, 단순한 mark로 구성합니다.
- 업무 단계와 분석 화면을 명확히 분리합니다.
- 현재 단계는 primary 배경 또는 primary outline으로 표시합니다.
- 완료 단계는 check 아이콘과 낮은 채도의 success 색상을 사용합니다.
- 잠긴 기능은 클릭할 수 없는 요소로 표시하고 `오후 실습`, `준비 중` 등의 이유를 같이 씁니다.
- 사이드바 하단의 기준월·프로토타입 상태는 작게 유지하되 대비를 확보합니다.

### 5.2 Topbar

- 왼쪽: 현재 업무 영역의 eyebrow와 한국어 제목
- 오른쪽: 기준월도, 환경 상태, 필요 시 사용자/알림 액션
- 페이지 제목과 분석 제목의 크기·위치를 통일합니다.
- topbar에서 업무 데이터를 계산하거나 조회하지 않습니다.

### 5.3 Page heading

```text
EYEBROW
페이지 제목
한 문장 설명                                      [상태 배지/주요 액션]
```

설명은 “무엇을 보여주는가”와 “왜 중요한가”를 한 문장으로 전달합니다. 제목 옆에 긴 안내문이나 여러 개의 배지를 배치하지 않습니다.

### 5.4 KPI 카드

KPI는 숫자 하나를 크게 보여주고, 그 숫자의 기준과 상태를 바로 아래에 설명합니다.

```text
라벨                    [상태 아이콘]
107.2M
전월 대비 +8.4% · 기준월 2026.09
```

- 숫자 단위는 숫자 안에 섞지 않고 시각적으로 분리할 수 있습니다.
- 증가·감소는 방향 아이콘과 텍스트를 함께 사용합니다.
- 데이터가 없으면 `—`를 표시하고, 가능한 경우 `데이터 준비 필요`를 표시합니다.
- KPI의 숫자를 컴포넌트에서 평균·분위수로 계산하지 않습니다.
- 클릭 가능한 KPI는 hover, focus, `aria-label`을 제공하고, 클릭 후 이동 위치를 예측할 수 있어야 합니다.

### 5.5 Card / Panel

- 제목, 보조 설명, 본문 순서를 일관되게 유지합니다.
- 제목이 없는 장식용 패널을 만들지 않습니다.
- 패널 안의 강조 영역은 surface-high 또는 의미 색상 container를 사용합니다.
- 패널마다 서로 다른 radius와 shadow를 임의로 조합하지 않습니다.

### 5.6 상태 배지와 태그

기존 `.tag`, `.local-badge`를 확장해 사용합니다. 색상만 바꾸는 별도 클래스 난립을 피합니다.

```text
.tag.green  → 정상·완료
.tag.blue   → 진행·정보
.tag.amber  → 대기·주의
.tag.red    → 위험·예외
.tag.gray   → 미정·잠금
```

배지는 짧은 명사형 문구를 사용합니다. `정상입니다`보다 `정상`, `확인이 필요합니다`보다 `확인 필요`가 적합합니다.

### 5.7 Tabs

- 현재 선택된 탭은 surface-highest 또는 primary outline으로 구분합니다.
- 잠긴 탭은 링크가 아닌 `span`으로 렌더링해 404와 준비 전 상태를 구분합니다.
- 탭 라벨에는 기능명만 쓰고 긴 설명은 본문으로 이동합니다.
- 분석 탭과 업무 단계 탭은 같은 pill 언어를 공유하되, 서로 다른 목적임을 heading으로 구분합니다.

### 5.8 Buttons

| 유형 | 시각 | 용도 |
|---|---|---|
| Primary | primary 배경, 높은 대비 | 다음 단계, 저장, 확정 |
| Secondary | surface-high + outline | 보조 이동, 미리보기 |
| Ghost | 투명 배경, 약한 텍스트 | 덜 중요한 액션 |
| Destructive | danger 계열 | 삭제·취소 등 되돌리기 어려운 작업 |
| Disabled | 낮은 대비, `not-allowed` | 아직 연결되지 않은 Phase 2 기능 |

- 버튼 문구는 동작으로 시작합니다: `저장`, `확정`, `다음 단계`, `보고서 보기`.
- 아이콘만 있는 버튼은 tooltip 또는 `aria-label`이 필수입니다.
- 로딩 상태에서는 중복 제출을 막고 `처리 중` 상태를 보여줍니다.

### 5.9 Table

표는 데이터 밀도를 확보하되, 어두운 배경에서 행 경계를 잃지 않게 합니다.

- 헤더는 surface-high 또는 bottom border로 본문과 구분합니다.
- 숫자 열은 오른쪽 정렬하고 `tabular-nums`를 사용합니다.
- 긴 텍스트는 잘라내기 전에 상세 확인 방법을 제공합니다.
- 상태 열은 배지로 표시하고 색상만으로 판정하지 않습니다.
- 빈 배열은 `표시할 데이터가 없습니다.`와 원인 점검 안내를 구분합니다.
- 조회 오류는 `조회에 실패했습니다.`와 실제 오류 메시지를 별도 영역에 표시합니다.
- 분석 표는 `components/analysis/data-table.tsx`를 재사용하고 컬럼별 `render`를 사용합니다.

### 5.10 Forms

- label은 input 위에 표시하고 placeholder를 label 대신 사용하지 않습니다.
- focus는 primary outline 또는 2px ring으로 명확하게 표시합니다.
- 오류는 필드 가까이에 한국어 메시지와 오류 아이콘을 표시합니다.
- 숫자 입력은 단위와 허용 범위를 함께 표시합니다.
- 저장 전/저장 중/저장 완료/저장 실패 상태를 구분합니다.
- 임시 값과 확정 값을 같은 스타일로 보이지 않게 합니다.

## 6. 화면별 적용 기준

### 6.1 홈 `/`

현재 `ProcurementApp`의 단계 구조는 유지하고 아래 순서로 스타일을 변경합니다.

1. 전체 배경과 `.main`을 다크 surface 체계로 전환
2. sidebar를 `surface-low` 계열로 조정
3. topbar를 반투명 surface + 약한 하단 경계로 변경
4. progress를 패널 안의 compact stepper로 재정렬
5. KPI 카드와 업무 카드에 공통 panel 규칙 적용
6. 현재 단계·완료 단계·대기 단계를 동일한 상태 색상 체계로 통일

업무 화면은 실제 연결 전까지 샘플 데이터임을 배지나 보조 문구로 명확히 합니다. 샘플 값이 실제 분석 결과처럼 오해되지 않도록 `LOCAL PROTOTYPE`, `PREVIEW`, `Phase 1` 표기를 유지합니다.

### 6.2 수요 확정

- 탭: `OL`, `SFDC`, `Bulk-deal`, `Trend`, `수급회의`
- 표 입력: surface-high 배경, focus outline, 명확한 label
- 확정 후보·조건부·참고·제외는 상태 배지와 select의 텍스트를 함께 사용
- 수요 확정 버튼은 primary, 확정 완료 상태는 success
- 현재 브라우저 상태만 바뀌는 부분은 저장 완료처럼 표현하지 않고 `로컬 미리보기`로 표시

### 6.3 재고·공급

- 현재고, 입고예정, 가용재고를 KPI 또는 요약 행으로 구분
- Open PO의 `가용`, `위험`, `후속월`을 상태 배지로 표시
- 리드타임·운송·통관·검수는 동일한 정보 카드 패턴을 사용
- 재고 부족은 danger, 입고일 미정은 warning, 정상 가용은 success

### 6.4 마스터 검증

- 품목, BOM, 장착율, MOQ, Lead Time, Flexibility Rule을 같은 크기의 검증 카드로 표시
- 준비됨/입력 필요의 상태를 카드 상단 또는 하단에서 고정 위치에 표시
- 검증 결과는 체크리스트와 상태 요약을 함께 제공합니다.
- 업로드 버튼이 비활성화된 동안은 비활성 이유와 활성화 예정 Phase를 표시합니다.

### 6.5 발주량 계산

- 계산 결과 표에서 `확정수요 → 가용재고 → 순소요량 → 최종발주량 → 예외`의 읽기 순서를 유지
- 최종발주량은 굵게, 계산량과 수동 조정량은 향후 별도 열로 구분
- Flex, MOQ, 납기 예외는 표 안의 배지와 표 아래 검토 순서를 함께 제공
- 계산 불가 값은 숫자 대체값을 사용하지 않고 `—`와 사유를 표시

### 6.6 보고자료

- 핵심 금액과 전월·전년·OL 대비를 우선 배치
- 보고서 미리보기는 실제 파일이 생성되지 않았으면 `생성 예정`으로 표시
- Excel/PDF 버튼은 disabled 상태에서도 왜 사용할 수 없는지 설명
- 보고용 숫자는 단위, 기준월, 데이터 확정 상태를 함께 보여줍니다.

### 6.7 분석 `/analysis/*`

- `AnalysisLayout`, `AnalysisFrame`, `AnalysisTabs`, `DataTable`의 공통 구조를 유지
- 페이지 제목은 홈의 workflow 제목보다 한 단계 작은 분석 heading을 사용
- `SUPABASE LIVE` 배지는 실제 Supabase 조회 화면에만 사용
- 분석 결과가 빈 배열이면 스키마 노출·권한 점검 문구를 제공
- 오류와 빈 결과를 같은 화면으로 합치지 않음
- 분석 KPI와 표는 DB가 반환한 결과를 표시하고 화면에서 통계 계산을 추가하지 않음

## 7. 접근성·상태·콘텐츠 기준

### 접근성

- 본문과 보조 텍스트의 대비를 다크 배경에서도 충분히 확보합니다.
- focus-visible 상태를 마우스 hover와 별도로 제공합니다.
- 버튼·링크·표·입력의 HTML 의미를 유지합니다.
- 아이콘만 있는 조작에는 `aria-label`을 제공합니다.
- 색상 상태에는 텍스트 또는 아이콘을 동반합니다.
- 모션은 150~250ms 정도의 짧은 전환만 사용하고, `prefers-reduced-motion`을 존중합니다.

### 상태 표현 순서

모든 비동기·데이터 화면은 다음 상태를 고려합니다.

```text
loading → success with data
        → success with empty result
        → error
        → unavailable / not configured
```

상태별 기본 문구:

- 로딩: `데이터를 불러오는 중입니다.`
- 빈 결과: `표시할 데이터가 없습니다.`
- 오류: `조회에 실패했습니다.`
- 미설정: `Supabase 환경변수를 확인해 주세요.`
- 계산 불가: `—` + `NO_USAGE` 또는 `NO_LEADTIME`

### 콘텐츠 톤

- 업무 용어는 한국어를 우선하고 필요할 때 괄호로 영문을 병기합니다.
- 제목은 설명형보다 목적형으로 씁니다: `리드타임 격차`, `재고·Open PO 확인`.
- 오류는 사용자를 탓하지 않고 다음 행동을 안내합니다.
- 숫자 단위는 `원`, `대`, `EA`, `일`, `%`를 일관되게 사용합니다.
- 샘플 데이터와 실제 데이터의 차이를 명확히 표시합니다.

## 8. 구현 매핑

### 우선 변경 파일

| 순서 | 파일 | 변경 내용 |
|---:|---|---|
| 1 | `app/globals.css` | 색상·간격·radius 토큰 추가, 기존 공통 클래스의 다크 테마 매핑 |
| 2 | `app/layout.tsx` | 필요 시 폰트 로딩과 전역 메타데이터만 조정 |
| 3 | `components/procurement-app.tsx` | 구조를 유지하고 클래스·접근성 속성·상태 표현을 정리 |
| 4 | `components/workflow/*.tsx` | 단계별 패널·표·상태 배지를 공통 토큰에 맞춤 |
| 5 | `components/analysis/*.tsx` | 분석 패널·탭·표를 홈과 같은 surface 언어로 통일 |
| 6 | `app/analysis/*/page.tsx` | 제목·오류·빈 결과·KPI 배치를 디자인 기준에 맞춤 |

### 구현하지 않는 것

- Tailwind CDN 또는 Tailwind 설정 추가
- Material Symbols 폰트 추가
- 별도 디자인 시스템 패키지 설치
- 차트 라이브러리 도입만을 위한 의존성 추가
- 디자인 변경을 이유로 업무 컴포넌트와 데이터 계층을 대규모로 재작성
- 화면 컴포넌트 안에 통계 계산 추가

## 9. 적용 순서와 검증 체크리스트

### 단계별 적용 순서

1. 전역 토큰과 `body`, 기본 panel 스타일을 적용합니다.
2. `/`의 sidebar·topbar·progress·KPI를 변경합니다.
3. 업무 단계 하나를 기준 화면으로 정해 표·폼·배지를 적용합니다.
4. 나머지 workflow 단계에 동일 패턴을 확장합니다.
5. `/analysis/leadtime`에 동일한 panel·table·state 규칙을 적용합니다.
6. 모바일 폭과 키보드 focus를 확인합니다.
7. `npm run test`, `npm run build`를 실행합니다.

### 완료 기준

- [ ] 홈과 분석 화면에서 배경·surface·텍스트 색상이 같은 토큰을 사용한다.
- [ ] primary, success, warning, danger의 의미가 모든 화면에서 동일하다.
- [ ] 카드·버튼·배지·탭의 모서리와 내부 간격이 임의로 달라지지 않는다.
- [ ] 숫자 열이 오른쪽 정렬되고 금액·수량·일수가 세로로 비교된다.
- [ ] 오류·빈 결과·미설정·계산 불가 상태가 서로 다르게 표시된다.
- [ ] 색상만으로 상태를 판단해야 하는 요소가 없다.
- [ ] 639px 이하에서도 주요 업무 액션과 표 데이터에 접근할 수 있다.
- [ ] Tailwind·새 CSS 프레임워크·새 아이콘 폰트가 추가되지 않았다.
- [ ] 기존 Supabase 스키마와 조회 경계가 유지된다.
- [ ] `npm run test`가 통과한다.
- [ ] `npm run build`가 성공한다.

## 10. 디자인 결정 기록

### 결정 1 — 다크 테마를 기본으로 사용

레퍼런스의 관제형 인상을 가져오되, 정보량이 많은 SCM 화면에서 surface 계층과 텍스트 대비를 충분히 확보합니다.

### 결정 2 — 글래스 효과는 보조 수단으로 사용

패널의 정체성을 투명도에만 의존하지 않습니다. 브라우저 지원 여부와 데이터 가독성을 위해 불투명 fallback surface를 기본으로 둡니다.

### 결정 3 — 기존 컴포넌트 경계를 유지

전반적인 디자인 변경이 도메인 조회·계산 로직까지 흔들지 않도록 CSS 토큰과 공통 UI 패턴을 중심으로 변경합니다.

### 결정 4 — 레퍼런스의 영어 정보 구조를 한국어 업무 문맥으로 번역

큰 숫자, 짧은 라벨, 상태 배지, compact 탭이라는 구조는 유지하지만, 업무 사용자가 즉시 이해할 수 있는 한국어 제목과 보조 문구를 사용합니다.

